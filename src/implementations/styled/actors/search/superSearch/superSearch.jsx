import { withStyles } from '@material-ui/styles';
import SuperSearch from '../../../../actors/search/superSearch';

const styles = {
    root: {
        display: 'flex'
    }
};

export default withStyles(styles)(SuperSearch);
