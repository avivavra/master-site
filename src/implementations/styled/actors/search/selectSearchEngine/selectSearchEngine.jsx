import { withStyles } from '@material-ui/core';
import SelectSearchEngine from '../../../../actors/search/selectSearchEngine';

const styles = {
    root: {
        width: '10rem'
    }
};

export default withStyles(styles)(SelectSearchEngine);
